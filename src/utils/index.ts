import vw, {vwMobile, vwTablet, vwDesktop, pxDesktop, pxTablet, pxMobile} from './vw';
import config from './config';

export {
    vw, vwMobile, vwTablet, vwDesktop, pxDesktop, pxTablet, pxMobile,
    config,
}